<!DOCTYPE html>
<html>
	<head>
		<meta http-equiv="Content-type" content="text/html, charset=iso-8815"/>
		<title>{% block title%}My Store{% endblock %}</title>
		{% block stylesheets %}
			<link href="{{ asset('css/style.css') }}" rel="stylesheet" type="text/css"/>
		{% endblock %}
	</head>
	<body>
		<div id="content">
			<div id="header">
				<h1>My Store</h1>
				<p align="right">
						{% if app.user %}
							Bonjour, <a href="{{ path('compte') }}">{{ app.user.username }}</a> | <a href="{{ path('logout') }}">Se déconnecter</a>
						{% else %}
							<a href="{{ path('login') }}">Compte</a> 
						{% endif %}
						| <a href="#">Panier</a>
				</p>
			</div>
			<div><a href="{{ path('home') }}">Accueil</a></div>
			{% block body %}
	
		 	{% endblock %}
		</div>
	</body>
</html>
